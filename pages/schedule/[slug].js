import React from "react";
import Layout from "../../common/Layout";
import Hero from "../../common/Hero";

const ScheduleGame = (props) => {
  console.log({ props });
  return (
    <Layout>
      <Hero image="" text="game"></Hero>
    </Layout>
  );
};

export default ScheduleGame;
